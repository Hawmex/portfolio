{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "My portfolio website.",
  "private": true,
  "scripts": {
    "prod": "npx tsc && npx esbuild dist/index.js --splitting --bundle --target=es2020 --outdir=public/build --format=esm --external:*.woff2 --minify",
    "dev:server": "cd public && npx live-server",
    "dev:tsc": "npx tsc --watch",
    "dev:esbuild": "npx esbuild dist/index.js --watch --splitting --bundle --target=es2020 --outdir=public/build --format=esm --external:*.woff2"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Hawmex/portfolio.git"
  },
  "keywords": [],
  "author": "Hamed Aarab",
  "bugs": {
    "url": "https://github.com/Hawmex/portfolio/issues"
  },
  "homepage": "https://github.com/Hawmex/portfolio#readme",
  "dependencies": {
    "lit-html": "^1.4.1",
    "nexinterface": "^3.3.4",
    "nexwidget": "^5.1.1"
  },
  "devDependencies": {
    "esbuild": "^0.12.23",
    "typescript": "^4.4.2"
  }
}
